diff --git a/src/core/ChainSolver.js b/src/core/ChainSolver.js
index b269ee63610152dfa6b891d7896f7729938f0480..005b025ef93c61b1fbcd3a4f5d5ccf9cb05c4f3e 100644
--- a/src/core/ChainSolver.js
+++ b/src/core/ChainSolver.js
@@ -223,6 +223,16 @@ export class ChainSolver {
 
 			}
 
+			// Joint limits can leave an unconverged chain with no remaining
+			// variables. Treat that state as stalled instead of multiplying a
+			// matrix with zero rows.
+			if ( freeDoF === 0 ) {
+
+				status = SOLVE_STATUS.STALLED;
+				break;
+
+			}
+
 			// Check if we've diverged
 			if ( totalError > prevErrorMagnitude + divergeThreshold ) {
 
